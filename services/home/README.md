# Home Landing Page service

The `Home App` provides the landing page for the demo server.

## Architecture

Simple HTML webapp via nginx HTTP server Docker Image 
with static webpages and config mounted into the Container.
